build:
  type: mvn-quarkus-jib
  properties:
    container-name: extensions/shopify-extension
    container-tag: latest
deploy:
  type: container-deploy
  properties:
    endpoint-prefix: shopify-extension
    endpoint-accessibility: public