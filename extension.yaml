build:
  type: mvn-quarkus-jib
  properties:
    container-name: extensions/shopify
    container-tag: latest
deploy:
  type: container-deploy
  properties:
    endpoint-prefix: shopify
    endpoint-accessibility: public